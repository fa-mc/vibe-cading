# Session backlog / parking lot

Short-lived items, ideas, and parked refactors that surfaced during a session
but are not actively being driven. Curated by PM. Promote to
[`.agents/plans/INDEX.md`](.agents/plans/INDEX.md) when an item earns a
design slot.

## Pre-OSS publication checklist (release-blockers for v1)

Added 2026-05-14 per user direction. These are gates on the v1 OSS publication — not deferred features. Owner: human contributor at release-prep time; PM curates status. Promote any non-trivial item to [`.agents/plans/INDEX.md`](.agents/plans/INDEX.md) before execution.

- **Top-level `README.md` + getting-started.** File exists ([README.md](README.md), 125 lines, modified locally and uncommitted) but needs a release-readiness audit. Per the 2026-05-14 TL Round 7 dialog, the README must explain: this is a parametric CAD library + AI-agent workflow; primary use = clone the repo and open in Claude Code (or another agent host that imports `vibe/INSTRUCTIONS.md`); the library `vibe_cading/` is agent-agnostic and can be used programmatically without agents if desired. Distribution is **git-clone-only** (no pip / PyPI in v1 — see Deferred features below). Getting-started flow must walk a new contributor from clone → `tools/init-claude-runtime.sh` (or host-equivalent) → first `tools/view.py` or `python build.py` invocation. Confirm the existing local edits cover this before staging.
- **`CONTRIBUTING.md` audit.** File exists ([CONTRIBUTING.md](CONTRIBUTING.md), 101 lines, modified locally and uncommitted). Verify it covers: the multi-role agentic workflow (link to [docs/agentic-workflow.md](docs/agentic-workflow.md)), the AGPLv3 header requirement for new files under `vibe_cading/` / `parts/` / `tools/`, the design-brief → plan-index flow in `.agents/plans/`, the no-`__main__`-blocks rule (CI-enforced), the print-profile / tolerance system, and the CLA workflow (`.github/workflows/cla.yml` already in place). Same release-readiness pass as README.
- **`examples/` directory.** Greenfield — does NOT exist yet. Should contain a small set of self-contained CadQuery scripts (or `tools/view.py`-invocable model classes) that demonstrate the library without requiring agent-host setup. Candidates: a Lego-Technic beam adapter, a generic screw-cutter usage, a `Gear.from_iso(...)` preview, a snap-fit hook with `.to_cutter()` cut into a host body. Each example MUST run with `python3 examples/<name>.py` against a vanilla CadQuery install and print where it wrote its STEP/SVG. Cross-reference from README's getting-started section.
- **GitHub Actions CI — release-readiness pass.** CI exists ([.github/workflows/ci.yml](.github/workflows/ci.yml), 63 lines, modified locally and uncommitted; plus `cla.yml` and `engine-api.yml`). Confirm coverage before publication: `flake8` lint, `tools/check_license_headers.py`, `tools/check_no_main_blocks.py`, `tools/check_topology.py`, full `python build.py` smoke (or representative subset), and any preview / section-slice asset regen checks. Audit pinning of action versions and Python version; add a badge to README once stable.
- **Author `LICENSE-FAQ.md` at repo root.** Greenfield — does NOT exist yet ([LICENSE](LICENSE) is present; FAQ is not). AGPLv3 is unfamiliar territory for many CAD / hardware users, so a plain-language FAQ reduces contributor friction and avoids inbox noise. Recommended coverage: (1) why AGPLv3 vs. MIT / Apache — copyleft protects derivative parametric models from closed-source fork; (2) what AGPLv3 means specifically for users who **print** parts (no source-distribution trigger — printing is not "conveying" the software); (3) what it means for users who **modify and share** model classes (must publish source under AGPLv3); (4) what it means for users who **expose vibe_cading over a network service** (the "A" — must offer source to remote users); (5) the relationship between AGPLv3 and the CLA (`.github/workflows/cla.yml`); (6) common false-positive concerns (using the library to design a part you sell — fine; embedding `vibe_cading` in a closed-source desktop app — not fine). Link from README and CONTRIBUTING. Owner: human contributor at release-prep time.
- **Purge non-publishable docs from working tree AND git history.** [docs/business-strategy.md](docs/business-strategy.md) (added in commit `0a7a562`, "docs: add business strategy and monetization plan") MUST NOT ship in the public repo — confidential commercial content. Audit the rest of [docs/](docs/) for the same publication test before release: currently present are [docs/archive/mcp-integration-plan.md](docs/archive/mcp-integration-plan.md), [docs/knowledge_base/mcp_architecture_rules.md](docs/knowledge_base/mcp_architecture_rules.md), [docs/templates/design-brief-template.md](docs/templates/design-brief-template.md), [docs/agentic-workflow.md](docs/agentic-workflow.md), [docs/lego-technic.md](docs/lego-technic.md), [docs/screws.md](docs/screws.md) — each needs an explicit ship/no-ship verdict. **Working-tree delete is not enough**: history-rewrite (`git filter-repo --invert-paths --path docs/business-strategy.md ...`, repeated for any other flagged docs) before pushing the publication remote. **Coordination required**: this is a destructive history-rewrite — must happen as the final step before publication, after all collaborators have pushed outstanding work and acknowledged the rewrite. Owner: human contributor (Admin role); needs explicit one-off confirmation per the base instructions' force-push prohibition.

## Deferred features (gated on real demand)

- **Pip-installable distribution + PyPI publication.** Deferred 2026-05-14 by Admin diagnosis: Option C (pip-installable) was a single-adapter speculative seam — one hypothetical platform consumer doesn't justify HE Phase 1 effort + ongoing PyPI maintenance. The renamed `vibe_cading/` namespace is structurally pip-ready (a future PR adds `pyproject.toml` + `[tool.setuptools.packages.find].exclude` for `parts`/`experiments` + version metadata; ships to PyPI). **Trigger condition to revisit:** a second real consumer is committed (NOT hypothetical — the platform team or another downstream actively asking for `pip install vibe-cading`). Until then, distribution = `git clone`.

## Admin follow-ups

- **Upstream proposal: fifth false-positive carve-out for `core-agents:structural-optimization`.** The skill's deletion test currently has four canonical carve-outs (observability / versioning / security-boundary / cold-start-orientation). The 2026-05-13 PM↔user dialog on this project surfaced a legitimate fifth shape: **contributor-extension contract** — a class with no maintainer-side polymorphism that still earns its keep because external OSS contributors inherit / implement it for IDE auto-completion, `@abstractmethod` enforcement, or documented protocol shape. Project-local enforcement is in place (see `vibe/INSTRUCTIONS.md` → Code Quality & Open-Source Standards → Deep-Modules Dual-Lens Rule). Upstream path: route through `bump-review` skill once the next core-agents release window opens, or open a PR / issue against `fa-mc/core-agents` directly. Owner: Admin (human contributor, given the project does not ship a tracked Admin persona).

## Parked refactors — from 2026-05-09 TL deep-modules review

- **Rename `SlipperGearBase` → `SlipperGearAssembly`** ([models/xlego/slipper_gear/directional/base.py](models/xlego/slipper_gear/directional/base.py)). The class has no abstract methods and no extension hooks; the `Base` suffix misleads readers into expecting an inheritance pattern that does not exist. Naming nit, not structural depth.
- **Harmonize `models/mechanical/__init__.py` re-exports**. Currently exports `ClearanceHole`/`CounterboreHole`/`TeardropHole` only — not screws, nuts, joints, or gears, even though those subpackages re-export at their own level. Either re-export all top-level domain types from `models.mechanical` or remove the partial set so the public-API boundary is consistent.
- **Audit `cq_utils.tapered_arm_profile` / `archimedean_spiral_arc` for second-adapter justification**. Both are used by `slipper_gear` only today — single-adapter speculative seam under the deep-modules diagnostic. If no second consumer materializes, fold them back into the slipper-gear module.
