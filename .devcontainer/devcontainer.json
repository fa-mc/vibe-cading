{
    "name": "CadQuery Dev",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "bernhard-42.ocp-cad-viewer"
            ]
        }
    },
    "forwardPorts": [
        3939
    ],
    "remoteUser": "root",
    "mounts": [
        "source=${localEnv:HOME}/.ssh,target=/tmp/host-ssh,type=bind,readonly"
    ],
    "remoteEnv": {
        "SSH_AUTH_SOCK": "/ssh-agent",
        "PYTHONPATH": "/workspaces/cad/models"
    },
    "postCreateCommand": "mkdir -p ~/.ssh && cp -a /tmp/host-ssh/. ~/.ssh/ && chown -R $(whoami) ~/.ssh && chmod 700 ~/.ssh && chmod 600 ~/.ssh/* && git config --global --add safe.directory /workspaces/vcad"
}